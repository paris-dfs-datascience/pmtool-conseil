firebase login --no-localhost

# Build and push to Container Registry
gcloud builds submit --tag gcr.io/lma-website-461920/lma-chat-api

# Deploy to Cloud Run
gcloud run deploy lma-chat-api \
  --image gcr.io/lma-website-461920/lma-chat-api \
  --set-env-vars GITHUB_TOKEN=$GITHUB_TOKEN \
  --region us-central1 \
  --platform managed \
  --allow-unauthenticated \
  --memory 1Gi \
  --cpu 1 \
  --max-instances 1

git add .
git commit -m "added video player to home page. Need to create and add video"
git push -u origin main



